# __PLUGIN_NAME__

A descriptor-only no-auth connector plugin scaffolded by `tai plugins init`.

Replace the placeholder identifiers (`acme`), endpoints (`example.invalid`), the
icon, and the `config_fields` in `tai-plugin.yml` with your provider's real
values, then edit `docs/index.mdx` before publishing.

Validate the descriptor and its docs with the marketplace tooling, and inspect the
schema with `tai plugins schema`.
