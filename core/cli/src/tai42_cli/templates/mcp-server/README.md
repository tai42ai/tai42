# __PLUGIN_NAME__

A descriptor-only MCP server plugin scaffolded by `tai plugins init`.

Replace the placeholder identifier (`acme`), the endpoint (`example.invalid`), and
the icon in `tai-plugin.yml` with your server's real values, then edit
`docs/index.mdx` before publishing.

Validate the descriptor and its docs with the marketplace tooling, and inspect the
schema with `tai plugins schema`.
