spec_version: 1
namespace: acme
name: __PLUGIN_NAME__
version: 0.0.0
display_name: Acme
description: Descriptor-only MCP server for Acme.
icon: https://example.invalid/icon.png
license: Apache-2.0
homepage: https://example.invalid
repository: https://example.invalid
# An all-mcp-server spec declares no ``contract`` and, descriptor-only, no
# ``package`` — nothing is pip-installed but the manifest entry.
categories: [integrations]
tags: [mcp-server]
permissions: {network: false, subprocess: false, filesystem: false}
provides:
  - kind: mcp-server
    name: acme
    description: Sample hosted MCP server for Acme.
    mcp:
      type: http
      url: https://example.invalid/mcp
