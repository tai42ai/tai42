"""The ``ask_user`` concern modules: the internal, skeleton-private pieces the
public ``ask_user`` orchestrator composes. Nothing outside the ``interactions``
package imports from here; public names stay reachable through ``helper`` and the
``interactions`` package."""
