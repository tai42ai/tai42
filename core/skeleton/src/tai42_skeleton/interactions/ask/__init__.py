"""The ``ask_user`` concern modules: the internal pieces the public orchestrator composes.

Nothing outside the ``interactions`` package imports from here; public names stay
reachable through ``helper`` and the ``interactions`` package.
"""
