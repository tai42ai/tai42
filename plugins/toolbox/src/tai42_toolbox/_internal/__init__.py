"""Private helper modules backing the toolbox tools and tool extensions.

Not part of the public API. Grouped by the artifact family they serve:
``extensions`` holds the helpers a registered entrypoint in
``tai42_toolbox.extensions`` delegates to, and ``tools`` holds those backing
``tai42_toolbox.tools``. Nothing here registers through ``tai42_app``.
"""
