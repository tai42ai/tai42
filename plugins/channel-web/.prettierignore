# Build outputs and artifacts — never formatted.
node_modules/
pnpm-lock.yaml
uv.lock

# The compiled chat-page bundle + generated manifest (emitted by
# scripts/build-public.mjs, shipped inside the Python package).
src/tai42_channel_web/public/
