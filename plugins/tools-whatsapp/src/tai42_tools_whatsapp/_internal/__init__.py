"""Private helper modules backing the WhatsApp provisioning tools.

Not part of the public API. ``tools`` holds the shared Meta Graph client the thin registered
entrypoints in ``tai42_tools_whatsapp.tools`` delegate to. Nothing here registers through
``tai42_app``.
"""
