## What

<!-- What does this PR change? Name the package(s) under core/, plugins/, or e2e/. -->

## Why

<!-- Why is the change needed? Link related issues. -->

## Checklist

- [ ] Tests pass (`uv run --package tai42-<name> pytest --cov --cov-report=term-missing`)
- [ ] Lint is clean (`uv run --package tai42-<name> ruff check .`)
